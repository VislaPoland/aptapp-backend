version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports:
    - "5432:5432"
    environment:
      POSTGRES_PASSWORD: aptapp
      POSTGRES_DB: aptapp
      POSTGRES_USER: aptapp